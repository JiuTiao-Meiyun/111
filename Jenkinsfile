pipeline {
    agent any
    stages {
        stage('运行Python') {
            steps {
                // 直接调用系统Python运行脚本
                sh 'python test.py'  // 替换为您的脚本路径
            }
        }
    }
}
